#!/bin/sh

. bin/variables

# Stop running containers and remove images (-v parameter can be added to also remove volumes)
${DOCKER_DIRECTORY}/bin/compose/stop --rmi all "$@"

# Remove stopped containers
docker-compose rm
